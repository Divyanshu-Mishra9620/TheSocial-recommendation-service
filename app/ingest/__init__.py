"""Event ingestion for the Recommendation Service.

Intentionally empty so importing the package has no side effects (it does not
pull the Redis client). The consumer runs as a standalone worker.
"""
