# Offline training dependencies (NOT installed in the serving image).
# Install for running the training pipeline:
#   pip install -r requirements-train.txt
-r requirements.txt

pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
xgboost==2.1.3
pyarrow==18.1.0
APScheduler==3.10.4
